package com.backend.blue_lock.core.shared.entities

enum class EnumStatus(val value: Int) {
    //Used when the register was created
    Created(0),

    //Used when the register was deleted
    Deleted(2),
}